/**
 * Timing constants for UI interactions and debouncing
 */
export const TIMING = {
  // Auto-sync intervals
  DEBOUNCE_TIME: 200,
  SYNC_INTERVAL: 200,
  
  // UI update delays
  UI_UPDATE_DELAY: 100,
  SCROLL_DELAY: 50,
  
  // API timeouts
  DEFAULT_API_TIMEOUT: 30000,
  BRANCH_CREATION_TIMEOUT: 45000,
  
  // Retry intervals
  RETRY_DELAY: 1000,
  MAX_RETRY_ATTEMPTS: 3
};